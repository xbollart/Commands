
Empty commit:
git commit --allowempty -m "initial commit"

Regular commit:
git commit -m "first commit"

Add file to commit:
git add

push commit to repo:
git push

Read list of changes:
git fetch

Store dirty changes for switching to a new branch:
git stash

Pull and rebase changes:
git pull --rebase

Display all info:
git log

Display editable changes:
git log --oneline

Display unmutable changes:
git reflog

Display branch:
git branch -a

Switch branch:
git checkout "branch name"

Display repo status without untracked 
git status -u

Desactivate ssl certificat:
git config --global http.sslVerify false

Delete commits:
git reset --hard

Template .gitignore
https://raw.githubusercontent.com/github/gitignore/master/VisualStudio.gitignore

unstage change
git reset head

Move last 3 commits to a branch:
git branch newbranch
git reset --hard HEAD~3
git push -f

change commit message localy:
git commit --amend

Count nb of file modified:
git status | grep 'modified:' | wc -l

